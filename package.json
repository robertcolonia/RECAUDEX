{
  "name": "recaudex-fullstack",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:local": "npm run db:up && npm run db:deploy && npm run db:seed && npm run dev",
    "build": "npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "eval:agents": "npm run eval:agents -w backend",
    "db:generate": "npm run prisma:generate -w backend",
    "db:up": "docker-compose up -d db",
    "db:deploy": "npm run prisma:deploy -w backend",
    "db:migrate": "npm run prisma:migrate -w backend",
    "db:seed": "npm run db:seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}
